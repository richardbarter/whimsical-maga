; PHP overrides for local development.
; Mounted into the container by docker-compose.yml.

; Show errors directly in the browser/response
display_errors=On
error_reporting=E_ALL

; Disable OPcache so PHP file changes are picked up immediately without restarting the container
opcache.enable=0
opcache.enable_cli=0
